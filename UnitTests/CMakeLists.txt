# This file was generated by Jucer2Reprojucer from "UnitTestRunner.jucer"

cmake_minimum_required(VERSION 3.4)

project("UnitTestRunner")


list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}/../../external/FRUT/prefix/FRUT/cmake")
include(Reprojucer)


set(UnitTestRunner_jucer_FILE
  "${CMAKE_CURRENT_LIST_DIR}/UnitTestRunner.jucer"
)


jucer_project_begin(
  JUCER_VERSION "5.4.7"
  PROJECT_FILE "${UnitTestRunner_jucer_FILE}"
  PROJECT_ID "Z2Xzcp"
)

jucer_project_settings(
  PROJECT_NAME "UnitTestRunner"
  PROJECT_VERSION "1.0.0"
  COMPANY_NAME "ROLI Ltd."
  COMPANY_COPYRIGHT "ROLI Ltd."
  REPORT_JUCE_APP_USAGE OFF # Required for closed source applications without an Indie or Pro JUCE license
  DISPLAY_THE_JUCE_SPLASH_SCREEN OFF # Required for closed source applications without an Indie or Pro JUCE license
  PROJECT_TYPE "Console Application"
  BUNDLE_IDENTIFIER "com.roli.UnitTestRunner"
  CXX_LANGUAGE_STANDARD "C++14"
  PREPROCESSOR_DEFINITIONS
    "JUCE_UNIT_TESTS=1"
)

jucer_project_files("UnitTestRunner/Source"
# Compile   Xcode     Binary    File
#           Resource  Resource
  .         .         .         "Source/PolySynthUnitTests.h"
  x         .         .         "Source/Main.cpp"
)

jucer_project_files("UnitTestRunner"
# Compile   Xcode     Binary    File
#           Resource  Resource
  x         .         .         "../Source/PolySynth.cpp"
  .         .         .         "../Source/PolySynth.h"
)

jucer_project_module(
  juce_analytics
  PATH "../../external/JUCE/modules"
)

jucer_project_module(
  juce_audio_basics
  PATH "../../external/JUCE/modules"
)

jucer_project_module(
  juce_audio_devices
  PATH "../../external/JUCE/modules"
  # JUCE_USE_WINRT_MIDI
  # JUCE_ASIO
  # JUCE_WASAPI
  # JUCE_WASAPI_EXCLUSIVE
  # JUCE_DIRECTSOUND
  # JUCE_ALSA
  # JUCE_JACK
  # JUCE_BELA
  # JUCE_USE_ANDROID_OBOE
  # JUCE_USE_ANDROID_OPENSLES
  # JUCE_DISABLE_AUDIO_MIXING_WITH_OTHER_APPS
)

jucer_project_module(
  juce_audio_formats
  PATH "../../external/JUCE/modules"
  # JUCE_USE_FLAC
  # JUCE_USE_OGGVORBIS
  # JUCE_USE_MP3AUDIOFORMAT
  # JUCE_USE_LAME_AUDIO_FORMAT
  # JUCE_USE_WINDOWS_MEDIA_FORMAT
)

jucer_project_module(
  juce_audio_processors
  PATH "../../external/JUCE/modules"
  # JUCE_PLUGINHOST_VST
  # JUCE_PLUGINHOST_VST3
  # JUCE_PLUGINHOST_AU
  # JUCE_PLUGINHOST_LADSPA
)

jucer_project_module(
  juce_audio_utils
  PATH "../../external/JUCE/modules"
  # JUCE_USE_CDREADER
  # JUCE_USE_CDBURNER
)

jucer_project_module(
  juce_blocks_basics
  PATH "../../external/JUCE/modules"
)

jucer_project_module(
  juce_core
  PATH "../../external/JUCE/modules"
  # JUCE_FORCE_DEBUG
  # JUCE_LOG_ASSERTIONS
  # JUCE_CHECK_MEMORY_LEAKS
  # JUCE_DONT_AUTOLINK_TO_WIN32_LIBRARIES
  # JUCE_INCLUDE_ZLIB_CODE
  # JUCE_USE_CURL
  # JUCE_LOAD_CURL_SYMBOLS_LAZILY
  # JUCE_CATCH_UNHANDLED_EXCEPTIONS
  # JUCE_ALLOW_STATIC_NULL_VARIABLES
  JUCE_STRICT_REFCOUNTEDPOINTER ON
)

jucer_project_module(
  juce_cryptography
  PATH "../../external/JUCE/modules"
)

jucer_project_module(
  juce_data_structures
  PATH "../../external/JUCE/modules"
)

jucer_project_module(
  juce_dsp
  PATH "../../external/JUCE/modules"
  # JUCE_ASSERTION_FIRFILTER
  # JUCE_DSP_USE_INTEL_MKL
  # JUCE_DSP_USE_SHARED_FFTW
  # JUCE_DSP_USE_STATIC_FFTW
  # JUCE_DSP_ENABLE_SNAP_TO_ZERO
)

jucer_project_module(
  juce_events
  PATH "../../external/JUCE/modules"
  # JUCE_EXECUTE_APP_SUSPEND_ON_BACKGROUND_TASK
)

jucer_project_module(
  juce_graphics
  PATH "../../external/JUCE/modules"
  # JUCE_USE_COREIMAGE_LOADER
  # JUCE_USE_DIRECTWRITE
  # JUCE_DISABLE_COREGRAPHICS_FONT_SMOOTHING
)

jucer_project_module(
  juce_gui_basics
  PATH "../../external/JUCE/modules"
  # JUCE_ENABLE_REPAINT_DEBUGGING
  # JUCE_USE_XRANDR
  # JUCE_USE_XINERAMA
  # JUCE_USE_XSHM
  # JUCE_USE_XRENDER
  # JUCE_USE_XCURSOR
  # JUCE_WIN_PER_MONITOR_DPI_AWARE
)

jucer_project_module(
  juce_gui_extra
  PATH "../../external/JUCE/modules"
  # JUCE_WEB_BROWSER
  # JUCE_ENABLE_LIVE_CONSTANT_EDITOR
)

jucer_project_module(
  juce_opengl
  PATH "../../external/JUCE/modules"
)

jucer_project_module(
  juce_osc
  PATH "../../external/JUCE/modules"
)

jucer_project_module(
  juce_product_unlocking
  PATH "../../external/JUCE/modules"
)

jucer_project_module(
  juce_video
  PATH "../../external/JUCE/modules"
  # JUCE_USE_CAMERA
  # JUCE_SYNC_VIDEO_VOLUME_WITH_OS_MEDIA_VOLUME
)

jucer_export_target(
  "Xcode (MacOSX)"
)

jucer_export_target_configuration(
  "Xcode (MacOSX)"
  NAME "Debug"
  DEBUG_MODE ON
  BINARY_NAME "UnitTestRunner"
  ADD_RECOMMENDED_COMPILER_WARNING_FLAGS "Enabled"
  OSX_DEPLOYMENT_TARGET "10.10"
  CXX_LANGUAGE_STANDARD "GNU++14"
)

jucer_export_target_configuration(
  "Xcode (MacOSX)"
  NAME "Release"
  DEBUG_MODE OFF
  BINARY_NAME "UnitTestRunner"
  ADD_RECOMMENDED_COMPILER_WARNING_FLAGS "Enabled"
  OSX_DEPLOYMENT_TARGET "10.10"
  CXX_LANGUAGE_STANDARD "GNU++14"
)

jucer_export_target(
  "Linux Makefile"
)

jucer_export_target_configuration(
  "Linux Makefile"
  NAME "Debug"
  DEBUG_MODE ON
  BINARY_NAME "UnitTestRunner"
  EXTRA_LIBRARY_SEARCH_PATHS
    "/usr/X11R6/lib/"
)

jucer_export_target_configuration(
  "Linux Makefile"
  NAME "Release"
  DEBUG_MODE OFF
  BINARY_NAME "UnitTestRunner"
  EXTRA_LIBRARY_SEARCH_PATHS
    "/usr/X11R6/lib/"
)

jucer_export_target(
  "Visual Studio 2017"
  EXTRA_COMPILER_FLAGS
    "/w44265"
    "/w45038"
    "/w44062"
)

jucer_export_target_configuration(
  "Visual Studio 2017"
  NAME "Debug"
  DEBUG_MODE ON
  BINARY_NAME "UnitTestRunner"
  TREAT_WARNINGS_AS_ERRORS ON
)

jucer_export_target_configuration(
  "Visual Studio 2017"
  NAME "Release"
  DEBUG_MODE OFF
  BINARY_NAME "UnitTestRunner"
  TREAT_WARNINGS_AS_ERRORS ON
  DEBUG_INFORMATION_FORMAT "None"
)

jucer_export_target(
  "Visual Studio 2019"
  EXTRA_COMPILER_FLAGS
    "/w44265"
    "/w45038"
    "/w44062"
)

jucer_export_target_configuration(
  "Visual Studio 2019"
  NAME "Debug"
  DEBUG_MODE ON
  BINARY_NAME "UnitTestRunner"
  TREAT_WARNINGS_AS_ERRORS ON
)

jucer_export_target_configuration(
  "Visual Studio 2019"
  NAME "Release"
  DEBUG_MODE OFF
  BINARY_NAME "UnitTestRunner"
  TREAT_WARNINGS_AS_ERRORS ON
  DEBUG_INFORMATION_FORMAT "None"
)

jucer_project_end()
